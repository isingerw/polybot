# ============================================
# Analytics Service 基础配置
# 分析服务：提供 ClickHouse 数据查询 API
# ============================================

spring:
  # 应用名称
  application:
    name: analytics-service
  # 激活的开发环境配置
  profiles:
    active: develop

# 服务器端口配置
server:
  port: 8082
